<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="31"
        android:label="@string/app_name"
        android:theme="@style/Theme.Skicket">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Skicket">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
 <activity android:name=".BuySkipassActivity" />
 <activity android:name=".TicketsActivity" />
        <activity android:name=".TicketDetailsActivity" />
        <activity android:name=".MoreOptionsActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".SkidomainActivity" />
        <activity android:name=".DomainTicketActivity" />
    </application>
</manifest>